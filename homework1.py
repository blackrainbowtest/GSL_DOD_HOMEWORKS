u_tuple = (10, 20, 30, 40, 50, 60)
u_tuple2 = (1, 3, 5, 7, 9)

print(f'First half values: {u_tuple[:len(u_tuple) // 2]} Last half values: {u_tuple[len(u_tuple) // 2:]}')
print(f'First half values: {u_tuple2[:len(u_tuple2) // 2]} Last half values: {u_tuple2[len(u_tuple2) // 2:]}')
